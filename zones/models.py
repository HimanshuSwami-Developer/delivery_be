# The `Zone` model that used to live here has been removed — this app
# served a single dark store, and everything that referenced it (Order,
# DeliveryPartner, ProductStock, Coupon) now stands on its own. Kept as an
# empty app only until its final migration (deleting the `Zone` table) has
# been applied and the app itself is removed from INSTALLED_APPS.
